name: "Upload failure Artifacts"
description: "Upload failure Artifacts"
inputs:
  name:
    description: ""
    required: true
runs:
  using: "composite"
  steps:
    - name: pack failure artifacts
      shell: bash
      run: |
        echo "Current running processes"
        ps uax
        echo "Processes that access current dir"
        lsof +D `pwd` || true
        killall sccache || true
        killall sccache-dist || true
        # possible temp dirs for either linux or windows
        cp "${TMP:-${TEMP:-${TMPDIR:-/tmp}}}"/sccache*.{log,txt} . 2>/dev/null || true
        mkdir -p target
        tar --exclude='target' \
            --exclude='docs' \
            --exclude='bins' \
            --exclude='.git' \
            --exclude='*.profraw' \
            -czvf 'target/failure-${INPUTS_NAME}.tar.gz' .
      env:
        INPUTS_NAME: ${{ inputs.name }}
    - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
      with:
        name: '${{ inputs.name }}'
        path: 'target/failure-${{ inputs.name }}.tar.gz'
