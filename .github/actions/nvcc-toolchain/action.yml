name: nvcc-toolchain
inputs:
  cuda-version:
    description: CUDA Toolkit version
    required: true

runs:
  using: composite
  steps:
    - if: runner.os == 'Linux'
      shell: bash
      run: .github/actions/nvcc-toolchain/install-cuda.sh ${INPUTS_CUDA_VERSION}
      env:
        INPUTS_CUDA_VERSION: ${{ inputs.cuda-version }}

    - if: runner.os == 'Windows'
      shell: powershell
      run: .\.github\actions\nvcc-toolchain\install-cuda.ps1 -cudaVersion $env:INPUTS_CUDA_VERSION
      env:
        INPUTS_CUDA_VERSION: ${{ inputs.cuda-version }}
